[uwsgi]
HOME = /home/deploy

master = true
daemonize = %(HOME)/var/log/uwsgi/%n.log
pidfile = %(HOME)/var/run/uwsgi/%n.pid
workers = 1
uid = deploy
gid = deploy
socket = %(HOME)/var/run/uwsgi/%n.sock
chmod-socket = 666
single-interpreter = true

# Clean up socket and pid files on exit
vacuum = true

# Kill requests that take too long
harakiri = 30

# Reload when memory usage gets too high
reload-on-rss = 256

# Ensure POST bodies are read
post-buffering = 1

chdir = %(HOME)/apps/%n/current
wsgi-file = application.wsgi
